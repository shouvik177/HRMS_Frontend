# Backend API base URL (no trailing slash).
# Leave empty to use local storage only (no API).
# Production backend:
VITE_API_URL=https://hrms-backend-8v9r.onrender.com

# Login is required by default. Set to "true" only to allow access without login (e.g. dev/single-admin).
# VITE_SKIP_AUTH=true
